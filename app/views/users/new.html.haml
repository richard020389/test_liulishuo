%h1 Sign Up
= form_for @user do |f|
  = f.label(:name,"用户名")
  = f.text_field(:name)

  = f.label(:password,"密码")
  = f.password_field(:password)

  = f.label(:password_confirmation,"密码确认")
  = f.password_field(:password_confirmation)

  .button=f.submit("确认")



